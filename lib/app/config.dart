import '../enum/app_enum.dart';

class AppConfig {
  static const AppMode appMode = AppMode.local;
  static const NetworkDebug networkDebug = NetworkDebug.close;
  static const String apiKey = '';
  static const String apiUrl = '';
}
